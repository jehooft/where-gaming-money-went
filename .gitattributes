# Normalise to LF everywhere. The Pages workflow re-runs the data build and compares
# the result against what's committed, so a CRLF checkout on Windows would otherwise
# make that check fail for no real reason.
* text=auto eol=lf

*.png binary
*.jpg binary
*.pdf binary
